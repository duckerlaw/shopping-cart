import HeaderCart from "./HeaderCart";

const Header = ()=> {
    return (
        <header className="header">
            <h1>Biyxkii Shop</h1>
            <HeaderCart />
        </header>
    );
}

export default Header;